package lcmtypes;

struct goal_list_t
{
    int64_t utime;

    int64_t sender_id; // used so senders can tell who is sending what....

    int32_t num_goals;
    lcmtypes.goal_t	goals[num_goals];

}
